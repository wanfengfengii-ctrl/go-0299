# 生态研学建筑现代夯土承重墙承梁准入质量闭环

## 项目目标

建设一个可持久化、可重启恢复的 Go HTTP 后端，以“墙段—水平层—压实单元”的只追加夯筑格为核心模型，管理现代夯土承重墙从设计与配方锁定、土料筛分调湿、分盘拌合、模板验收、逐层夯筑、拉结埋设、养护检测、异常返筑、双人复核到承梁准入的完整证据链。实现规模限定为约 20—28 个生产 Go 文件、2000—3000 行有效生产代码，目标约 2500 行，划分为至少 4 个有业务意义的包；采用嵌入式事务数据库保存聚合、材料账本、租约、仪器调用、检测证据、代次与终局裁定，并在启动时恢复未完成任务、到期租约和待重试调用。所有状态推进使用客户端操作号、锁定摘要、任务代次和单调逻辑时间校验；所有质量计算使用带溢出检查的整数定点运算，使并发测试、故障测试与边界测试能够精确复现。

## 端到端业务流程

1. 锁定任务：提交建筑区、墙段几何、门窗洞口、拉结筋与保护区、水平层和压实单元划分、土源及筛分批次、整数克配方、目标含水率、拌合盘计划、松铺厚度、夯击程序、模板拉杆、养护日程、试块映射、扫描网格、质量阈值和竖向承压邻接表；服务校验覆盖、交叠、禁区、尺寸与乘加溢出后生成不可变设计摘要和任务代次。
2. 备料与分盘：按依赖完成筛分、留样、含水调节和分盘拌合；一个事务内校验并扣减原土、砂砾、稳定剂和水，登记留样、合理损耗及回收料，同时获取搅拌机和含水率仪限时互斥租约。每盘生成不可合并的材料代次、确定的制成逻辑时间和可用截止时间。
3. 模板与逐层夯筑：模板区验收通过后，压实单元只能沿墙段锁定方向追加连续前缀；布料、各夯击遍次、标高复测和拉结件埋设分别形成不可变证据。开始上层前必须闭合下层全部材料盘映射、夯击、标高与拉结证据，租约过期、盘次错误、混合料恰好越过期限或工序越序均拒绝写入。
4. 养护与检测：逐层记录湿度均衡养护时间轴；到期后按试块映射和测点网格调用电子秤、含水率仪、夯击计数器、扫描仪或压力机，计算含水率、干密度、压实系数、单位体积夯击能、层间剪切强度、侵蚀失重率和垂直偏差。仪器拒绝、断连、超时或格式错误仅建立确定重试次数的待重试调用，不生成读数或推进状态。
5. 异常返筑：检测到密度不足、含水率越界、冷缝、分层裂缝、拉结偏位、层间剪切不足或侵蚀超限时，依据已锁定的竖向承压路径、层间接触面及同盘材料关系计算唯一返筑集合；一次性隔离该集合，建立新代次重新覆盖受影响层及相邻承压单元，并永久保留旧代次材料去向、检测证据与作废原因。
6. 复核与终局：返筑和全部证据闭合后，由两名不同且具备资格的人员独立复核；承梁准入、结构隔离和取消通过单写终局屏障竞争，成功者生成唯一终局记录，失败并发请求读取同一结果或得到稳定冲突码，任何旧代次迟到回执不得改变终局。

## 核心组件与职责

1. 夯土构造与材料规则目录：管理不可变锁定摘要，校验墙段、洞口、拉结禁区、分层网格、压实单元连续覆盖、承压邻接、材料配方、检测阈值及整数毫米边界。
2. 墙段分层任务及压实聚合：实现墙段—水平层—压实单元只追加格、连续前缀、跨层闭合条件、任务代次、逻辑时间和重启后的聚合重放。
3. 材料守恒与资源租约管理器：以整数克复式账本维护各组分、留样、有效入墙量、回收料和合理损耗；通过数据库事务原子完成材料领用与搅拌机、模板区、夯具、含水率仪、扫描通道的限时互斥租约。
4. 拌合夯筑及养护证据记录器：记录筛分、调湿、分盘、模板、布料、夯击遍次、标高、拉结件和养护事件，管理混合料材料代次与可用期限，并持久化脚本化仪器调用及待重试状态。
5. 检测返筑与终局仲裁器：执行定点质量计算、阈值判定、返筑集合传播、代次隔离、双人复核和承梁准入、结构隔离、取消的单写终局裁定。
6. Go HTTP API：提供版本化 JSON 接口、事务边界、幂等操作处理、稳定错误码、确定性原因排序、健康检查及恢复状态查询。

## 领域规则与不变量

1. 每个锁定任务的设计、配方、工序、网格、阈值和邻接表均不可变；后续命令必须携带匹配的设计摘要与当前任务代次，否则返回 DESIGN_DIGEST_STALE 或 GENERATION_STALE。
2. 层与压实单元必须以整数毫米连续覆盖墙体设计实体，扣除洞口且避开拉结保护区，不得缺失、非法交叠或退化；面积、体积及坐标乘加均需检查负值和 int64 溢出。
3. 每层仅能沿锁定方向形成压实单元连续前缀，每个单元的夯击遍次也必须连续；上层首个单元开始前，下层必须闭合材料、全部夯击遍次、标高复测和拉结证据。
4. 原土、砂砾、稳定剂、水、留样、回收料、有效入墙量和合理损耗按整数克守恒；调湿前后水量必须可追溯，每盘材料不可与其他盘合并，且只能在锁定的可用截止逻辑时间内使用。
5. 搅拌机、模板区、夯具、含水率仪和扫描通道采用排他租约；获取、续租、释放和过期回收以逻辑时间决定，材料扣减与所需租约获取必须在同一事务中成功或整体回滚。
6. 质量指标统一使用带比例因子的整数定点值和明确的半远离零舍入规则；校验符号、除零、时间间隔和中间乘加溢出，不允许浮点数参与裁定。
7. 异常返筑集合是锁定邻接关系、层间接触面、同盘关系和异常类型的纯函数结果；相同输入必须产生按建筑区、墙段、层号、单元、盘次、代次排序的唯一集合，新代次只接受自身回执。
8. 同一操作号与相同规范化内容重试返回原结果，与不同内容重用则返回 IDEMPOTENCY_CONFLICT；所有拒绝均使用稳定错误码和确定排序原因，失败事务不得留下材料扣减、租约、有效夯筑格、检测证据或准入凭据。

## 数据模型与持久化

1. LockedTask：任务 ID、建筑区、设计摘要、当前代次、锁定方向、逻辑时钟、状态及不可变规则版本。
2. WallGeometry：墙段、整数毫米边界、洞口、拉结筋及保护区、模板拉杆、水平层、压实单元、检测网格和竖向承压邻接边。
3. MaterialBatch 与 MixPan：土源、筛分批次、组分批次、整数克余额、配方、目标含水率、留样、损耗、回收料、材料代次、制成时间、截止时间和入墙映射。
4. MassLedgerEntry：事务号、账户类型、材料批次、盘次、代次、借贷方向、整数克数量、原因及逻辑时间；每笔业务事务借贷总和必须为零。
5. ResourceLease：资源类型与 ID、持有操作、租约令牌、取得时间、到期时间、状态和回收版本，资源类型与 ID 上建立唯一有效租约约束。
6. CompactionCell 与 EvidenceEvent：墙段、层号、单元序号、代次、工序、遍次、材料盘、逻辑时间、人员或仪器、定点值、证据摘要和有效性。
7. InstrumentCall：调用 ID、仪器类型、输入摘要、逻辑时间、尝试次数、最大次数、状态、稳定故障码、原始响应摘要及下次允许重试时间。
8. InspectionFinding 与 RebuildPlan：异常类型、测点或接触面、阈值结果、唯一返筑集合摘要、旧新代次、材料去向、隔离状态和完成状态。
9. Review 与 FinalVerdict：复核人员、资格摘要、独立结论、证据快照摘要，以及唯一的准入、结构隔离或取消终局、凭据号和写入版本。
10. IdempotencyRecord：作用域、操作号、规范化请求摘要、事务结果、HTTP 状态和响应摘要，用于跨重启重放一致结果。

## 公开接口

1. POST /v1/tasks/lock：锁定完整任务；返回任务 ID、设计摘要、代次和确定排序的几何或规则错误。
2. POST /v1/tasks/{id}/operations：统一提交筛分、调湿、拌合、模板验收、布料、夯击、标高、拉结和养护事件，要求操作号、设计摘要、代次和逻辑时间。
3. POST /v1/tasks/{id}/leases/acquire 与 /renew、/release：原子获取或管理资源租约；涉及备料时可在 operation 请求中与材料领用联合提交。
4. POST /v1/tasks/{id}/instruments/calls 与 /retry：登记脚本化仪器请求和确定性重试结果；GET /v1/tasks/{id}/instruments/pending 查询重启后待重试队列。
5. POST /v1/tasks/{id}/inspections 与 /rebuilds：写入有效检测证据、生成唯一返筑计划、启动新代次或查询返筑传播明细。
6. POST /v1/tasks/{id}/reviews 与 /verdicts：提交独立复核并竞争写入准入、结构隔离或取消终局；GET /v1/tasks/{id}/snapshot 返回按领域键确定排序的当前状态和证据摘要。
7. GET /healthz 与 GET /readyz：分别报告进程健康和数据库恢复、到期租约清理、待重试队列加载是否完成；所有接口使用稳定 JSON 错误结构 {code,reasons,operation_id}。

## 失败边界

1. 数据库事务是材料账本、租约、夯筑格、证据、代次和终局的原子边界；任一校验、唯一约束或持久化失败均整体回滚。
2. 设计摘要陈旧、层缺失或交叠、洞口或拉结禁区冲突、越序、错误盘次、失效混合料、租约过期和逻辑时间倒退在写入前拒绝。
3. 并发材料超领和租约争用依赖事务条件更新与唯一约束裁决，输家获得 MATERIAL_OVERCLAIM 或 LEASE_BUSY，不能观察到负库存或双持有。
4. 所有体积、密度、能量和比率计算先做范围检查；符号错误、除零或乘加溢出返回稳定计算错误码，且不保存部分指标。
5. 仪器边界仅接受结构化成功回执；拒绝、断连、超时和格式错误转为持久化待重试调用，重试次数及逻辑时间确定，不以模拟值补齐证据。
6. 返筑创建通过当前代次条件写入防止双创建；旧代次、迟到回执和内容冲突不能更新当前单元、检测结论或返筑完成状态。
7. 终局表对任务建立唯一约束并以比较当前版本的事务写入；并发准入、隔离和取消只能有一个成功，其余请求返回已形成的终局或 FINAL_CONFLICT。
8. 启动恢复在开放就绪接口前完成：重载未完成聚合，按持久化逻辑时间标记到期租约，恢复待重试调用；损坏摘要或账本不平衡时保持非就绪并报告 RECOVERY_INTEGRITY_FAILED。

## 验收标准

1. 锁定请求能固定墙体几何、洞口与拉结件、分层单元、材料批次与配方、盘计划、夯击养护程序、试块与检测网格、阈值、邻接表和任务代次，并拒绝陈旧摘要。
2. 整数毫米几何校验能确定识别实体覆盖缺口、非法交叠、洞口或拉结禁区冲突、层厚和垂直度越界、退化尺寸、负值及乘加溢出。
3. 并发备料可证明所有整数克账户守恒，材料扣减和租约获取原子完成；相同操作号同内容跨重启幂等，不同内容稳定冲突。
4. 筛分、调湿、分盘、模板、逐单元布料与夯击、标高、拉结和养护严格按锁定依赖推进，层与夯击遍次保持连续前缀，并正确处理混合料截止边界和租约到期。
5. 全部质量指标按规定比例和舍入规则得到精确整数结果；仪器故障只生成带逻辑时间、稳定错误码和确定尝试次数的待重试记录。
6. 各类质量异常按照承压路径、接触面和同盘关系一次生成唯一返筑集合，新代次覆盖受影响范围，旧代次证据和材料去向永久可查且迟到回执被隔离。
7. 材料、夯筑、养护、几何、扫描、试块、剪切及侵蚀证据全部合格并由两名不同合格人员复核后，仅生成一个承梁准入凭据；准入、隔离和取消并发时只有一个终局。
8. 仓库包含 go.mod、可编译运行的 Go 后端、嵌入式持久化与重启恢复、适配 linux/amd64 和 linux/arm64 的 Docker 构建配置；生产代码控制在至少 20 个 Go 文件和约 2000—3000 有效行，公开测试不少于 4 个文件、12 个独立用例。

## 确定性测试场景

1. 几何锁定表驱动测试：使用固定墙段、洞口、拉结保护区和公开网格，断言合法连续覆盖及缺层、交叠、退化尺寸、负坐标和面积溢出的稳定错误码与原因顺序。
2. 材料守恒测试：以整数克原土、砂砾、稳定剂和水执行筛分、留样、调湿、分盘、入墙、回收和损耗，逐事务断言借贷平衡与盘次不可合并。
3. 并发超领与租约竞争测试：同步屏障释放多个 goroutine 争抢同一库存和设备，精确断言唯一胜者数量、输家错误码、无负库存、无双租约和失败回滚。
4. 幂等与重启测试：相同操作号同内容重放得到字节等价业务结果，不同内容得到 IDEMPOTENCY_CONFLICT；关闭并重启服务后结果、未完成前缀和待重试队列保持一致。
5. 工序前缀测试：覆盖跳层、重复单元、跳过夯击遍次、下层证据未闭合、错误盘次、租约恰好到期以及混合料截止前、截止时和截止后的边界。
6. 定点计算测试：用公开整数体积、质量、遍次、夯击功、剪切荷载和侵蚀失重数据断言含水率、干密度、压实系数、能量、剪切、侵蚀及垂直偏差的精确舍入，并覆盖除零和溢出。
7. 仪器故障测试：脚本化拒绝、断连、超时、坏格式后成功的序列，断言失败不产生读数、不推进状态，尝试次数、逻辑时间和恢复后的下一次调用完全确定。
8. 返筑传播测试：分别注入密度不足、冷缝、裂缝、拉结偏位、剪切不足和侵蚀超限，按公开承压邻接表断言唯一集合、同盘扩展、相邻接触面扩展和确定排序。
9. 代次隔离测试：返筑建立新代次后并发提交旧代次检测与夯筑回执，断言均被拒绝且旧材料去向、旧证据和当前结论不变。
10. 双人复核测试：同一人员重复复核、无资格人员复核及两名合格人员独立复核，断言仅最后一种满足终局前置条件。
11. 终局竞争测试：用同步屏障并发提交承梁准入、结构隔离和取消，断言数据库中仅一条终局、仅一种凭据且所有响应可由胜者确定解释。
12. 综合事务回滚测试：在材料扣减、租约取得、夯筑格追加、检测证据写入和终局生成的预设故障点注入提交失败，逐项断言没有任何部分状态遗留。

## 组件追踪关系

1. 规则目录覆盖验收 1、2，负责锁定摘要、整数毫米几何、洞口与拉结禁区、网格和阈值校验。
2. 分层任务及压实聚合覆盖验收 1、4、6，负责只追加格、连续前缀、跨层闭合、逻辑时间和代次隔离。
3. 材料守恒与资源租约管理器覆盖验收 3、4，负责整数克账本、并发超领、原子租约及失败回滚。
4. 证据记录器覆盖验收 4、5，负责工序依赖、盘次期限、养护时间轴、仪器故障和持久化重试。
5. 检测返筑与终局仲裁器覆盖验收 5、6、7，负责定点计算、异常传播、双人复核及单写终局。
6. Go HTTP API 覆盖验收 1—8，统一暴露幂等命令、稳定错误、确定排序、状态查询、健康就绪和跨重启行为。

## 独特性

本项目独特地将现代夯土承重墙的整数克材料谱系、限时设备租约、方向性只追加夯筑格、竖向承压路径返筑传播和屋面承梁单写准入屏障组合为一个可恢复事务工作流，区别于既有混凝土、钢结构、幕墙或防火封堵类质量闭环。
